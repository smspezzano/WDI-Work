<form action="/planes/<%= @plane.id %>" method="post">
		<input type="hidden" name="_method" value="put">
       <label>Name:</label> <br>
       <input type="text" name="plane[name]" value="<%= @plane.name %>"><br>
       <label>Engine Type:</label> <br>
       <input type="text" name="plane[engine_type]" value="<%= @plane.engine_type %>"><br>
       <label>Description:</label> <br>
       <textarea name="plane[description]"><%= @plane.description %></textarea><br>
        <!-- Need this to pass Rails security function -->
        <%= token_tag form_authenticity_token %>
        
        <button> ReFeul!</button><br>
    </form>

    <!-- the: value=" @plane.... " part is used to edit the plane whose ID was passed. This displays the current plane information for the client-->

    <!-- <input type="hidden" name="_method" value="put"> Is a hack to tell the Rail server this is actually a put, since the browser only knows the get/post requests -->